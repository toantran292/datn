{
  "name": "datn-scripts",
  "version": "1.0.0",
  "description": "Utility scripts for DATN project",
  "scripts": {
    "seed": "ts-node seed-demo-data.ts",
    "seed:reports": "ts-node seed-reports.ts",
    "seed:all": "npm run seed && npm run seed:reports",
    "clear": "ts-node clear-demo-data.ts"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "mongodb": "^6.3.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20.10.0",
    "@types/pg": "^8.10.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
