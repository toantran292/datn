{
  "name": "datn-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "services/*",
    "packages/*",
    "scripts/*",
    "docs/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "dependencies": {
    "http-proxy-middleware": "^3.0.5"
  },
  "devDependencies": {
    "@types/http-proxy-middleware": "^1.0.0"
  }
}