# API Configuration
# Backend API base URL (PM service via nginx proxy)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/pm

# Note: The NEXT_PUBLIC_ prefix makes these variables available in the browser
# All sensitive keys should be kept on the backend only
# If running backend directly without nginx, use: http://localhost:3000
