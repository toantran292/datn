---
// How It Works section
---

<section class="py-16 bg-white">
  <div class="max-w-4xl mx-auto px-6">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-[#0F172A] mb-4">How it works</h2>
      <p class="text-lg text-[#475569]">
        Get up and running with integrations in just a few clicks
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <!-- Step 1 -->
      <div class="text-center">
        <div class="w-16 h-16 bg-[#FF8800] rounded-full flex items-center justify-center text-white text-xl font-bold mx-auto mb-6">
          1
        </div>
        <h3 class="text-xl font-semibold text-[#0F172A] mb-3">Enable in Settings</h3>
        <p class="text-[#475569]">
          Navigate to your workspace settings and browse available integrations. Click "Enable" on the tools you want to connect.
        </p>
      </div>

      <!-- Step 2 -->
      <div class="text-center">
        <div class="w-16 h-16 bg-[#00C4AB] rounded-full flex items-center justify-center text-white text-xl font-bold mx-auto mb-6">
          2
        </div>
        <h3 class="text-xl font-semibold text-[#0F172A] mb-3">Grant permissions</h3>
        <p class="text-[#475569]">
          Authorize UTS to connect with your external tools. We only request the minimum permissions needed for each integration.
        </p>
      </div>

      <!-- Step 3 -->
      <div class="text-center">
        <div class="w-16 h-16 bg-gradient-to-r from-[#FF8800] to-[#00C4AB] rounded-full flex items-center justify-center text-white text-xl font-bold mx-auto mb-6">
          3
        </div>
        <h3 class="text-xl font-semibold text-[#0F172A] mb-3">Use in projects & chat</h3>
        <p class="text-[#475569]">
          Start using your connected tools directly within UTS. Create tasks from messages, schedule meetings, and more.
        </p>
      </div>
    </div>

    <!-- Visual connector lines for desktop -->
    <div class="hidden md:block relative mt-8">
      <div class="absolute top-0 left-1/2 transform -translate-x-1/2 w-full max-w-2xl">
        <svg class="w-full h-8" viewBox="0 0 400 32" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M50 16 L150 16 M250 16 L350 16" stroke="#E2E8F0" stroke-width="2" stroke-dasharray="5,5"/>
          <circle cx="150" cy="16" r="4" fill="#00C4AB"/>
          <circle cx="250" cy="16" r="4" fill="#00C4AB"/>
        </svg>
      </div>
    </div>

    <div class="text-center mt-12">
      <p class="text-[#475569] mb-4">
        Need help setting up an integration?
      </p>
      <a
        href="#"
        class="text-[#00C4AB] hover:text-[#00B0A0] font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-[#00C4AB] focus:ring-offset-2 rounded"
      >
        View our setup guides →
      </a>
    </div>
  </div>
</section>
