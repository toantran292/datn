# ===================== INFRA PORTS (block 41xxx) =====================
PG_PORT=41000
REDIS_PORT=41100

MINIO_API_PORT=41200
MINIO_CONSOLE_PORT=41201

OPENSEARCH_PORT=41300
OS_DASHBOARDS_PORT=41301

KAFKA_PORT=41400
PANDAPROXY_PORT=41401

MAILHOG_HTTP_PORT=41500
MAILHOG_SMTP_PORT=41501

# ===================== SECRETS / CREDS =====================
# Postgres
PG_USER=uts
PG_PASSWORD=uts_dev_pw
PG_DB=uts_dev

# MinIO
MINIO_ROOT_USER=miniokey
MINIO_ROOT_PASSWORD=miniopw123

# OpenSearch
OPENSEARCH_INITIAL_ADMIN_PASSWORD=opensearchDev!123

# ===================== IDENTITY SERVICE (DEV in Docker) =====================
# App port inside container (Spring default 8080). Host port is mapped in compose.
IDENTITY_PORT=40000

# Security
IDENTITY_PWD_PEPPER=dev-pepper-or-empty

# Database (use docker network hostnames)
# Compose service name for Postgres is 'postgres' and internal port is 5432
IDENTITY_DB_URL=jdbc:postgresql://postgres:5432/identity
IDENTITY_DB_USER=${PG_USER}
IDENTITY_DB_PASSWORD=${PG_PASSWORD}

# Google OAuth2 (set your own client credentials)
IDENTITY_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
IDENTITY_GOOGLE_CLIENT_SECRET=your-google-client-secret

# RSA key paths inside the container workdir (/app)
IDENTITY_RSA_PRIVATE_KEY_PATH=/app/private.pem
IDENTITY_RSA_PUBLIC_KEY_PATH=/app/public.pem